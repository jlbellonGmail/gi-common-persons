# Unidad 08 — resumen

La migración fue corregida en el worktree para usar exclusivamente `persons.*` y el project ref real quedó documentado. La aplicación y verificación remotas no se ejecutaron: Supabase CLI 2.117.0 falla con `TransportError` al inicializar el login role. La unidad permanece bloqueada y no está `READY_FOR_PR`.

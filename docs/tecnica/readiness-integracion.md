# Readiness Integracion

La validación incluye pruebas del paquete, contrato Core, aislamiento,
concurrencia, auditoría, migración/RLS estática, suite del Template, supply
chain y CI. La PR final se deja `READY_FOR_PR`; el único HITL restante decide
MERGE o NO MERGE.
